# Managed by chezmoi
# Tools installed by `mise install` (run by chezmoi script).
[tools]
node = "lts"
pnpm = "latest"
go = "1.23"
kubectl = "latest"
k9s = "latest"

# Add more as needed, e.g.
# go = "1.23"
# python = "3.12"
